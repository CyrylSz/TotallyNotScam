KROK 1:
PLIK: "index.html"
OPERACJA: [ZASTĄP]
SZUKAJ:
                        <!-- TAB: DEPOSIT (Dynamic) -->
                        <div class="w-content active" id="tab-deposit">
                            <label style="margin-bottom:10px; display:block; color:var(--text-muted); font-size:11px;">WYBIERZ METODĘ PŁATNOŚCI:</label>
                            
                            <div class="pay-methods-grid compact" id="depositMethodsGrid">
                                <!-- Generated by JS -->
                            </div>
                            
                            <!-- Dynamic Content Container -->
                            <div id="depositDynamicContainer"></div>

                            <div class="deposit-form centered-form" style="padding:0;">
                                <label>Kwota wpłaty</label>
                                <div class="input-group">
                                    <span class="curr-prefix">$</span>
                                    <input type="number" id="depositAmount" value="100" placeholder="0.00">
                                </div>
                                <div class="quick-amounts">
TREŚĆ:
                        <!-- TAB: DEPOSIT (Dynamic) -->
                        <div class="w-content active" id="tab-deposit">
                            <!-- Removed obvious label -->
                            
                            <div class="pay-methods-grid compact" id="depositMethodsGrid">
                                <!-- Generated by JS -->
                            </div>
                            
                            <!-- Dynamic Content Container -->
                            <div id="depositDynamicContainer"></div>

                            <div class="deposit-form centered-form" style="padding:0;">
                                <label>Kwota wpłaty</label>
                                <div class="input-group" style="max-width: 180px; margin: 0 auto 15px auto;">
                                    <span class="curr-prefix">$</span>
                                    <input type="number" id="depositAmount" value="100" placeholder="0.00">
                                </div>
                                <div class="quick-amounts">

KROK 2:
PLIK: "index.html"
OPERACJA: [ZASTĄP]
SZUKAJ:
                        <!-- TAB: WITHDRAW -->
                        <div class="w-content" id="tab-withdraw">
                            <div class="withdraw-form centered-form">
                                <label>Metoda wypłaty</label>
                                <div class="pay-methods-grid compact">
                                    <div class="pm-item active"><i class="fab fa-cc-visa"></i> Visa</div>
                                    <div class="pm-item"><i class="fab fa-bitcoin"></i> Wallet</div>
                                </div>

                                <label>Kwota do wypłaty</label>
                                <div class="input-group">
TREŚĆ:
                        <!-- TAB: WITHDRAW -->
                        <div class="w-content" id="tab-withdraw">
                            <div class="withdraw-form centered-form">
                                <!-- Removed obvious label -->
                                <div class="pay-methods-grid compact" id="withdrawMethodsGrid">
                                    <!-- Generated by JS -->
                                </div>
                                <div id="withdrawDynamicContainer" style="margin-bottom: 20px;"></div>

                                <label>Kwota do wypłaty</label>
                                <div class="input-group">

KROK 3:
PLIK: "index.html"
OPERACJA: [ZASTĄP]
SZUKAJ:
                    <div class="rg-stat centered">
                         <div class="rg-label">Limit Dzienny</div>
                        <div class="rg-val negative">92%</div>
                    </div>
TREŚĆ:
                    <div class="rg-stat centered">
                         <div class="rg-label">Limit Dzienny</div>
                        <div class="rg-val"><a href="#" onclick="alert('Przekierowanie do ustawień limitów...')" style="font-size:10px; color:var(--text-muted); text-decoration:underline; opacity:0.7;">Ustal własne limity</a></div>
                    </div>

KROK 4:
PLIK: "app.js"
OPERACJA: [WSTAW_PO]
SZUKAJ:
let currentDepositMethod = 'visa';
TREŚĆ:
let currentWithdrawMethod = 'visa';

KROK 5:
PLIK: "app.js"
OPERACJA: [ZASTĄP]
SZUKAJ:
    // 3. Init Deposit Methods
    selectDepositMethod(currentDepositMethod);

    // 4. Populate Transfer Item Select
TREŚĆ:
    // 3. Init Deposit & Withdraw Methods
    selectDepositMethod(currentDepositMethod);
    selectWithdrawMethod(currentWithdrawMethod);

    // 4. Populate Transfer Item Select

KROK 6:
PLIK: "app.js"
OPERACJA: [WSTAW_PO]
SZUKAJ:
    dynamicContent.appendChild(formDiv);
}
TREŚĆ:

function selectWithdrawMethod(method) {
    currentWithdrawMethod = method;
    const container = document.getElementById('withdrawMethodsGrid');
    const dynamicContent = document.getElementById('withdrawDynamicContainer');
    if(!container || !dynamicContent) return;

    // Render Buttons
    const methods = [
        { id: 'visa', icon: 'fab fa-cc-visa', name: 'Visa' },
        { id: 'crypto', icon: 'fab fa-bitcoin', name: 'Crypto' }
    ];

    container.innerHTML = '';
    methods.forEach(m => {
        const div = document.createElement('div');
        div.className = `pm-item ${m.id === method ? 'active' : ''}`;
        div.onclick = () => selectWithdrawMethod(m.id);
        div.innerHTML = `<i class="${m.icon}"></i> ${m.name}`;
        container.appendChild(div);
    });

    // Render Dynamic Content
    dynamicContent.innerHTML = '';
    
    if (method === 'visa') {
        dynamicContent.innerHTML = `
             <div style="font-size:11px; color:#aaa; text-align:center; padding:10px; background:rgba(255,255,255,0.02); border-radius:6px;">
                Środki wrócą na kartę: <b style="color:white;">Visa •••• 4242</b><br>
                Czas realizacji: 1-3 dni robocze.
            </div>
        `;
    } else if (method === 'crypto') {
        dynamicContent.innerHTML = `
            <div class="input-group small" style="margin-bottom:5px;">
                <span class="curr-prefix" style="font-size:10px;">ADDR</span>
                <input type="text" placeholder="Wklej adres USDT (TRC20)..." style="font-size:11px;">
             </div>
             <div style="font-size:10px; color:var(--accent-orange); text-align:center;">Upewnij się, że sieć to TRC20.</div>
        `;
    }
}