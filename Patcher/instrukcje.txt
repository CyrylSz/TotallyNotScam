KROK 1:
PLIK: "app.js"
OPERACJA: [ZASTĄP]
SZUKAJ:
        slot.innerHTML = `
            <i class="fas ${item.icon} inv-item-icon" style="color: ${item.color};"></i>
            <div class="inv-slot-rarity-dot" style="background-color: ${rarityColor}; box-shadow: 0 0 5px ${rarityColor};"></div>
            ${badgeHtml}
        `;
TREŚĆ:
        slot.innerHTML = `
            <i class="fas ${item.icon} inv-item-icon" style="color: ${item.color};"></i>
            <div class="inv-item-name" style="color: ${item.color};">${item.name}</div>
            ${badgeHtml}
        `;

KROK 2:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* Zwiększona ikona dla większego slotu */
.inv-item-icon { font-size: 42px; filter: drop-shadow(0 4px 6px rgba(0,0,0,0.8)); transition: 0.2s; }
TREŚĆ:
/* Zwiększona ikona dla większego slotu */
.inv-item-icon { font-size: 42px; filter: drop-shadow(0 4px 6px rgba(0,0,0,0.8)); transition: 0.2s; margin-bottom: 15px; }

.inv-item-name {
    position: absolute;
    bottom: 5px;
    left: 2px;
    right: 2px;
    font-size: 9px;
    font-weight: 700;
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    text-shadow: 0 1px 2px black;
    opacity: 0.9;
    pointer-events: none;
}