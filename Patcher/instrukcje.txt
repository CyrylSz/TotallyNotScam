KROK 1:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* Left Grid - SCALED SLOT SIZE (150px) */
.inventory-grid-scroll {
    flex: 1;
    overflow-y: auto;
    padding: 20px;
    display: grid;
    /* Skalowanie do 150px + gap 15px */
    grid-template-columns: repeat(auto-fill, 150px);
    grid-auto-rows: 150px;
    gap: 15px;
    align-content: start;
    justify-content: center;
}
TREŚĆ:
/* Left Grid - SCALED SLOT SIZE (110px) */
.inventory-grid-scroll {
    flex: 1;
    overflow-y: auto;
    padding: 20px;
    display: grid;
    /* Skalowanie do 110px + gap 10px (Mieści 5 w rzędzie) */
    grid-template-columns: repeat(auto-fill, 110px);
    grid-auto-rows: 110px;
    gap: 10px;
    align-content: start;
    justify-content: center;
}

KROK 2:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* UNIVERSAL SLOT STYLE (150px) */
.inv-grid-slot, .pd-slot {
    width: 150px !important; /* WYMUSZONY ROZMIAR */
    height: 150px !important; /* WYMUSZONY ROZMIAR */
    background: rgba(0,0,0,0.4);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 16px !important; /* Większe zaokrąglenie */
TREŚĆ:
/* UNIVERSAL SLOT STYLE (110px) */
.inv-grid-slot, .pd-slot {
    width: 110px !important; /* WYMUSZONY ROZMIAR */
    height: 110px !important; /* WYMUSZONY ROZMIAR */
    background: rgba(0,0,0,0.4);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 12px !important; /* Mniejsze zaokrąglenie */

KROK 3:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* Zwiększona ikona dla slotu 150px */
.inv-item-icon { font-size: 70px; filter: drop-shadow(0 4px 8px rgba(0,0,0,0.8)); transition: 0.2s; margin-bottom: 20px; }
TREŚĆ:
/* Skalowana ikona dla slotu 110px */
.inv-item-icon { font-size: 45px; filter: drop-shadow(0 4px 8px rgba(0,0,0,0.8)); transition: 0.2s; margin-bottom: 15px; }

KROK 4:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* Grid Calculation for 3x4 (SCALED 150px):
   Slot Size: 150px
   Gap: 15px
   Step: 165px
   Column Steps: 0, 165px, 330px
   Row Steps: 0, 165px, 330px, 495px
   Total Width: 480px
   Total Height: 645px
*/

.pd-formation {
    position: relative;
    width: 480px;
    height: 645px; /* Scaled 3x4 */
}

.pd-silhouette {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 580px; /* Zwiększona sylwetka dla 150px */
    color: rgba(0,0,0,0.3);
    filter: drop-shadow(0 0 50px rgba(0,0,0,0.8));
    z-index: 0;
    pointer-events: none;
}

/* UNIVERSAL SLOT SIZE FORCE (Redundant but kept for overrides) */
.pd-slot {
    position: absolute;
    width: 150px !important;
    height: 150px !important;
    border-radius: 16px !important;
    box-shadow: inset 0 0 30px rgba(0,0,0,0.8);
    background: rgba(0,0,0,0.6);
}
.pd-slot .placeholder { font-size: 60px; opacity: 0.3; } /* Większy placeholder */

/* --- GRID POSITIONS 3x4 (Recalculated for 150px + 15px gap) --- */

/* ROW 1 (Y=0) */
.slot-glasses   { top: 0; left: 0; }
.slot-neck      { top: 0; left: 165px; z-index: 5; }
.slot-earpiece  { top: 0; left: 330px; }

/* ROW 2 (Y=165) */
.slot-watch     { top: 165px; left: 0; }
.slot-suit      { top: 165px; left: 165px; z-index: 4; }
.slot-cufflink  { top: 165px; left: 330px; }

/* ROW 3 (Y=330) */
.slot-ring      { top: 330px; left: 0; }
.slot-belt      { top: 330px; left: 165px; z-index: 3; }
.slot-pants     { top: 330px; left: 330px; }

/* ROW 4 (Y=495) */
.slot-vehicle   { top: 495px; left: 0; border-color: rgba(59, 130, 246, 0.4); }
.slot-shoes     { top: 495px; left: 165px; z-index: 2; }
.slot-hand      { top: 495px; left: 330px; border-color: rgba(16, 185, 129, 0.4); }
TREŚĆ:
/* Grid Calculation for 3x4 (SCALED 110px):
   Slot Size: 110px
   Gap: 10px
   Step: 120px
   Column Steps: 0, 120px, 240px
   Row Steps: 0, 120px, 240px, 360px
   Total Width: 350px
   Total Height: 470px
*/

.pd-formation {
    position: relative;
    width: 350px;
    height: 470px; /* Scaled 3x4 */
}

.pd-silhouette {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 400px; /* Zmniejszona sylwetka dla 110px */
    color: rgba(0,0,0,0.3);
    filter: drop-shadow(0 0 50px rgba(0,0,0,0.8));
    z-index: 0;
    pointer-events: none;
}

/* UNIVERSAL SLOT SIZE FORCE (Redundant but kept for overrides) */
.pd-slot {
    position: absolute;
    width: 110px !important;
    height: 110px !important;
    border-radius: 12px !important;
    box-shadow: inset 0 0 30px rgba(0,0,0,0.8);
    background: rgba(0,0,0,0.6);
}
.pd-slot .placeholder { font-size: 40px; opacity: 0.3; } /* Mniejszy placeholder */

/* --- GRID POSITIONS 3x4 (Recalculated for 110px + 10px gap) --- */

/* ROW 1 (Y=0) */
.slot-glasses   { top: 0; left: 0; }
.slot-neck      { top: 0; left: 120px; z-index: 5; }
.slot-earpiece  { top: 0; left: 240px; }

/* ROW 2 (Y=120) */
.slot-watch     { top: 120px; left: 0; }
.slot-suit      { top: 120px; left: 120px; z-index: 4; }
.slot-cufflink  { top: 120px; left: 240px; }

/* ROW 3 (Y=240) */
.slot-ring      { top: 240px; left: 0; }
.slot-belt      { top: 240px; left: 120px; z-index: 3; }
.slot-pants     { top: 240px; left: 240px; }

/* ROW 4 (Y=360) */
.slot-vehicle   { top: 360px; left: 0; border-color: rgba(59, 130, 246, 0.4); }
.slot-shoes     { top: 360px; left: 120px; z-index: 2; }
.slot-hand      { top: 360px; left: 240px; border-color: rgba(16, 185, 129, 0.4); }

KROK 5:
PLIK: "app.js"
OPERACJA: [ZASTĄP]
SZUKAJ:
function renderItemInSlot(slotElement, item) {
    // Podmień HTML slotu na ikonę przedmiotu
    let rarityColor = getRarityColor(item.rarity);
    
    // Zwiększono rozmiar ikony do 70px dla slotów 150px
    slotElement.innerHTML = `<i class="fas ${item.icon}" style="color: ${item.color}; font-size: 70px; filter: drop-shadow(0 0 5px ${item.color});"></i>`;
    slotElement.style.borderColor = item.color;
    slotElement.style.background = `rgba(${hexToRgb(item.color)}, 0.15)`;
    slotElement.style.boxShadow = `0 0 15px rgba(${hexToRgb(item.color)}, 0.4)`;
    slotElement.title = `${item.name} (Kliknij aby zdjąć)`;
}
TREŚĆ:
function renderItemInSlot(slotElement, item) {
    // Podmień HTML slotu na ikonę przedmiotu
    let rarityColor = getRarityColor(item.rarity);
    
    // Zmniejszono rozmiar ikony do 45px dla slotów 110px
    slotElement.innerHTML = `<i class="fas ${item.icon}" style="color: ${item.color}; font-size: 45px; filter: drop-shadow(0 0 5px ${item.color});"></i>`;
    slotElement.style.borderColor = item.color;
    slotElement.style.background = `rgba(${hexToRgb(item.color)}, 0.15)`;
    slotElement.style.boxShadow = `0 0 15px rgba(${hexToRgb(item.color)}, 0.4)`;
    slotElement.title = `${item.name} (Kliknij aby zdjąć)`;
}