KROK 1:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
.favorites-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    padding-top: 10px;
    /* Fix wysokości panelu */
    min-height: 100px;
    max-height: 140px; /* 1 Rząd */
    overflow-y: auto; 
    padding-right: 5px; 
    align-content: start; /* KLUCZOWE: Zapobiega rozciąganiu kart w pionie */
}
TREŚĆ:
.favorites-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    padding-top: 10px;
    overflow-y: auto; 
    padding-right: 5px; 
    align-content: start; 
}

/* FIX: Sztywne wymuszenie wysokości dla Ulubionych (1 Rząd) */
#favoritesGrid {
    height: 140px !important;
    min-height: 140px !important;
    max-height: 140px !important;
}

KROK 2:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
/* Nadpisanie specyficzne dla Popularnych Gier: 2 Rzędy */
#popularModesGrid {
    max-height: 275px !important; /* Idealna wysokość na 2 rzędy */
    min-height: 100px; /* Usuwamy wymuszenie 290px, aby karty się nie rozciągały */
}
TREŚĆ:
/* FIX: Sztywne wymuszenie wysokości dla Popularnych (2 Rzędy) */
#popularModesGrid {
    height: 275px !important;
    min-height: 275px !important;
    max-height: 275px !important;
}

KROK 3:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
.fav-placeholder {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 30px;
    color: var(--text-muted);
    gap: 10px;
    text-align: center;
    border: 1px dashed rgba(255,255,255,0.1);
    border-radius: 8px;
}
TREŚĆ:
.fav-placeholder {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%; /* Dopasowanie do rodzica */
    padding: 0 20px; /* Mniejszy padding by nie rozpychać */
    color: var(--text-muted);
    gap: 10px;
    text-align: center;
    border: 1px dashed rgba(255,255,255,0.1);
    border-radius: 8px;
    box-sizing: border-box;
}