KROK 1:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
.mc-lock-overlay {
    position: absolute;
    top: 5px;
    right: 5px;
    background: transparent;
    padding: 0;
    color: var(--accent-red);
    font-size: 18px;
    filter: drop-shadow(0 2px 4px rgba(0,0,0,1));
    z-index: 10;
}
TREŚĆ:
.mc-lock-overlay {
    position: absolute;
    top: 5px;
    right: 5px;
    background: transparent;
    padding: 0;
    color: var(--accent-red);
    font-size: 12px;
    filter: drop-shadow(0 2px 4px rgba(0,0,0,1));
    z-index: 10;
}

KROK 2:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
.inv-grid-slot.is-rank-locked {
    opacity: 0.7;
    border-style: dotted;
    border-color: var(--accent-red);
}

.inv-grid-slot.is-rank-locked .inv-item-icon {
    filter: grayscale(100%);
    opacity: 0.5;
}
TREŚĆ:
/* Usunięto efekty wizualne (grayscale/border) dla zablokowanych slotów - zostaje sama kłódka overlay */
.inv-grid-slot.is-rank-locked {
    /* Brak stylów modyfikujących slot */
}

.inv-grid-slot.is-rank-locked .inv-item-icon {
    /* Brak stylów modyfikujących ikonę */
}

KROK 3:
PLIK: "index.html"
OPERACJA: [WSTAW_PO]
SZUKAJ:
                            <label class="ms-check-row">
                                <input type="checkbox" value="Divine" onchange="filterMarket()" checked> 
                                <span class="dot-r divine"></span> Divine <i class="fas fa-lock lock-icon" id="lockDivine"></i>
                            </label>
                        </div>
                    </div>
TREŚĆ:
                    <div class="ms-section" style="margin-top: auto; padding-top: 20px; border-top: 1px solid rgba(255,255,255,0.05);">
                        <div style="font-size: 10px; color: #666; line-height: 1.4; font-style: italic;">
                            <i class="fas fa-lock" style="color: var(--accent-red);"></i> - Oznacza brak wymaganej rangi do <b>założenia</b> przedmiotu. Nadal możesz nim handlować w 100%.
                        </div>
                    </div>