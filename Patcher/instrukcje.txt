KROK 1:
PLIK: "style.css"
OPERACJA: [ZASTĄP]
SZUKAJ:
.eu-sidebar {
    width: 240px;
    background: rgba(0,0,0,0.2);
    border-right: 1px solid rgba(255,255,255,0.05);
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 15px;
}
TREŚĆ:
.eu-sidebar {
    width: 240px;
    background: rgba(0,0,0,0.2);
    border-right: 1px solid rgba(255,255,255,0.05);
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 15px;
    overflow: hidden; /* Clip anything spilling out */
}

/* Wymuszenie szerokości inputa URL w sidebarze */
.eu-sidebar .input-group {
    width: 100% !important;
    display: flex;
    box-sizing: border-box;
}
.eu-sidebar .input-group input {
    width: 0 !important; /* Hack: zmusza flexbox do kurczenia elementu */
    min-width: 0 !important;
    flex-grow: 1 !important;
}